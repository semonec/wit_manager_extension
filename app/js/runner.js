$(document).ready(() => {
  const currentUrl = window.location.href;
  if (currentUrl.match('witland.xyz/bbs/write.php')) {
    // in case of write article
    // write plugin should used in here
  } else {
    // none write case
  }
});